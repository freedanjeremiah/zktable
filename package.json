{
  "name": "zktable",
  "version": "0.1.0",
  "private": true,
  "description": "Trustless, privacy-preserving board-game SDK on Stellar",
  "packageManager": "pnpm@10.34.1",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "tsup": "^8.3.6",
    "@types/node": "^22.13.0"
  },
  "engines": {
    "node": ">=20"
  }
}
